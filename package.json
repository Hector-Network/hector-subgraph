{
  "name": "hector-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ wkich/hector-subgraph",
    "create-local": "graph create --node http://localhost:8020/ wkich/hector-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ wkich/hector-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wkich/hector-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
